<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ID Not Found - Error Page</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background: linear-gradient(135deg, #1e3c72, #2a5298);
            
            color: #fff;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            text-align: center;
            padding: 20px;
        }

        .error-container {
            background: rgba(0, 0, 0, 0.6);
            padding: 60px;
            border-radius: 20px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.7);
            max-width: 600px;
            width: 100%;
        }

        .error-code {
            font-size: 120px;
            font-weight: bold;
            color: #ff4c4c;
            margin-bottom: 20px;
        }

        .error-message {
            font-size: 22px;
            margin-bottom: 35px;
            line-height: 1.6;
        }

        .btn-home {
            background: #ff4c4c;
            color: #fff;
            padding: 15px 30px;
            text-decoration: none;
            border-radius: 10px;
            font-weight: bold;
            transition: background 0.3s ease;
        }

        .btn-home:hover {
            background: #ff1a1a;
        }

        @media (max-width: 480px) {
            .error-code {
                font-size: 90px;
            }

            .error-message {
                font-size: 18px;
            }

            .error-container {
                padding: 40px;
                max-width: 90%;
            }
        }
    </style>
</head>

<body>
    <div class="error-container">
        <div class="error-code">404</div>
        <div class="error-message">Oops! The ID you entered was not found.<br>Please check and try again.</div>
        <a href="/" class="btn-home">Go to Homepage</a>
    </div>
</body>

</html>
